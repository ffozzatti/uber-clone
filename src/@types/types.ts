export interface TypesDataProps {
    id: string;
    type: string;
    price: number;
    duration: number;  
  } 

  export interface CarsDataTypes {
    id: string,
    type: string,
    latitude: number,
    longitude: number,
    heading: number,
  } 